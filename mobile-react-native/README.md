# Sahay React Native starter

This folder contains a minimal React Native starter for Sahay that mirrors the core assistive app concept.

## Run locally

1. Install Expo CLI if needed:
   `npm install -g expo-cli`
2. From this folder, run:
   `npm install`
   `npm start`

This is a starter scaffold and can be expanded with full contact, reminder, and AI features.
